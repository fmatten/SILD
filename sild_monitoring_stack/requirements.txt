# SILD Monitoring Stack — Python-Abhaengigkeiten

# ---- Pflichtabhaengigkeit ----
# Prometheus-Exporter fuer sild_mllp_filter.py und sild_fhir_filter.py
prometheus_client>=0.19.0

# Hinweis: In sild_detector.py existiert ein Plug-in-Slot fuer cairn.sild
# (try/except-Import). Dieser Slot ist NICHT verkabelt und wird hier deshalb
# auch nicht als optionale Abhaengigkeit aufgefuehrt. DE-Basisprofile
# (sild_fhir_profiles_de.py, --profiles-de) benoetigen keine Zusatzpakete.
